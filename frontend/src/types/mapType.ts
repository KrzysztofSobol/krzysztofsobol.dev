export type mapRow = string;
export type mapData = mapRow[];

export interface mapParameters {
    grassWeight: number;
    seaWeight: number;
    coastCornerWeight: number;
    coastWeight: number;
}